name: package/oath

on:
  # Post-submit.
  push:
    branches: [ main ]

  # Pre-submit.
  pull_request:
    branches: [ main ]
    paths:
      - .github/workflows/package_oath.yaml
      - packages/oath/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.0
      - uses: dart-lang/setup-dart@v1.6.5
      - run: dart pub get
        working-directory: packages/oath
      - run: ./dev.sh check --packages packages/oath
      - run: ./dev.sh test --packages packages/oath
      - run: ./dev.sh coverage --packages packages/oath
      - uses: coverallsapp/github-action@v2.2.3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          base-path: packages/oath
          path-to-lcov: packages/oath/coverage/lcov.info
