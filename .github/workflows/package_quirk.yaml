name: package/quirk

on:
  # Post-submit.
  push:
    branches: [ main ]

  # Pre-submit.
  pull_request:
    branches: [ main ]
    paths:
      - .github/workflows/package_quirk.yaml
      - packages/quirk/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4.2.0
      - uses: dart-lang/setup-dart@v1.6.5
      - run: dart pub get
        working-directory: packages/quirk
      - run: ./dev.sh check --packages packages/quirk
      - run: ./dev.sh test --packages packages/quirk
      - run: ./dev.sh coverage --packages packages/quirk
      - uses: coverallsapp/github-action@v2.2.3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          base-path: packages/quirk
          path-to-lcov: ./packages/quirk/coverage/lcov.info
