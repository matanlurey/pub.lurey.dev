# Format: https://dart.dev/tools/pub/pubspec

name: mirage
description: |-
  Generate noise and patterns algorithmically for use in software, textures, and
  graphics.
version: 0.1.0+1
repository: https://github.com/matanlurey/mirage.dart

environment:
  sdk: ^3.4.0

dependencies:
  chaos: ^0.1.1
  meta: ^1.14.0

dev_dependencies:
  checks: ^0.3.0
  image: ^4.2.0
  oath: ^0.2.0
  path: ^1.9.0
  test: ^1.25.3
