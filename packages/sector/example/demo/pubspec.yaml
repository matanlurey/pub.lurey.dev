name: sector_demo
publish_to: 'none'

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  sector:
    path: ../../

dev_dependencies:
  flutter_test:
    sdk: flutter
  oath:
    path: ../../../oath

dependency_overrides:
  http_parser: any
  meta: any

flutter:
  uses-material-design: true
